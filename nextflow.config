
params.registry = null
docker.enabled  = true
docker.registry = params.registry

// Only relevant for Podman settings
profiles {
    podman {
        docker.enabled  = false
        podman.enabled  = true
        podman.registry = params.registry
    }
}
